apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: records-api
  namespace: nagp-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: records-api
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
